@model EmployeeWithMessageAndBranchListViewModel

@{
    ViewData["Title"] = "testViewModel";
}
@Context.Session.GetString("name"); @*get value from key from session*@

<h1>testViewModel</h1>
<div style="color:@Model.Color">
@Model.EmpName
<br />
@Model.EmpID
<br />
<select class="form-control">
    @foreach (var item in Model.Branches)
    {
        <option>@item</option>
    }
</select>
@(Model.Temp+10)
</div>

